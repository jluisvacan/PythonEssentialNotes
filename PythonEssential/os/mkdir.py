"""
    La funcion mkdir() del modulo os crea un directorio en una ruta especifica. Requiere de una ruta que sea relativa o absoluta

    os.mkdir("dir")
"""

import os

os.mkdir("my_first_directory")
print(os.listdir())
