"""
    El metodo sort(), al igual que la funcion sorted(), ordena la lista original afectando el array original.
    Genera un nuevo array.

    Ejemplo:
        array.sort()

"""

array = [ "Red", "Blue", "Green"]
array.sort()

print(array)