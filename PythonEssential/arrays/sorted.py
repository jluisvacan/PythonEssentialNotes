"""
    La funcion sorted() toma como argumento una lista y devuelve la lista ordenada. La lista original permanece intacta.
    Genera un nuevo array.

    Ejemplo:
        sorted(array)

"""

array = [ "Red", "Blue", "Green"]

print(sorted(array))
print(array)